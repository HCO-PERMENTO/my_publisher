[project]
name = "my_publisher"
version = "0.1.0"
description = "Et nyt Python-projekt til at publicere noget fantastisk"
authors = [ { name = "Dit Navn", email = "you@example.com" } ]
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.9"

[project.dependencies]
# Tilføj afhængigheder her, eks.:
# requests = "^2.31"

[tool.pytest.ini_options]
minversion = "7.0"
addopts = "-ra -q"
testpaths = ["tests"]
